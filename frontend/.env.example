# API Host Configuration
# For local development, leave empty to auto-detect
# For production, set to your server's IP or domain
VITE_API_HOST=

# Example for accessing from other devices on local network:
# VITE_API_HOST=192.168.1.100
